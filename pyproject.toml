[project]
name = "my-pass"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = ["flet==0.85.0", "cryptography==48.0.0"]

[project.urls]
repository = "https://github.com/wyc-z/my-pass"

[project.optional-dependencies]
dev = ["pytest==9.0.3"]
